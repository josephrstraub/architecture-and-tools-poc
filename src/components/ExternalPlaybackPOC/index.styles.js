import { StyleSheet } from 'react-native';
import Constants from '../../constants';

export const videoWidth: number = Constants.SCREEN_WIDTH;

export default StyleSheet.create({
  video: {
    height: 200,
    width: 375,
  },
});

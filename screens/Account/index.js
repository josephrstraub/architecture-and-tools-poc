/* @flow */
import * as React from 'react';
import { Text, View } from 'react-native';
import styles from './index.styles';

type Props = {};

export default function Account(props: Props): React.Element<*> {
  return (
    <View>
      <Text>This is the Account component!</Text>
    </View>
  );
}
